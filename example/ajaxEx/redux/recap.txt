redux의 컨셉은 크게 두가지다.
첫번째는 하나의 객체가 모든 데이터를 관리하는것.
캡슐화와 높은 응집성을 가진 것으로 보인다.

두번째는 이 객체가 가진 데이터는 오직 간접적인 접근만 가능하다는 점.
캡슐화가 깨지는 것을 방지하기 위한 것으로 보인다.

dispatcher or reducer > 세터의 역할
getstate > 게터의 역할

원본을 복제하고 그 복제한것을 원본으로 바꿔치움.
이 특징으로 이전상태로 되돌리기, 되돌린걸 취소하기를 쉽게 사용 가능

형상관리 + 객체지향론 + 루트개념 섞은느낌.

